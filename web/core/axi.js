class Axi {
  constructor(config) {
    this.name = config.name
    this.desc = config.desc
  }

  // Use a service
  use(service, config) {
    // ...
  }

  // Add a device
  add(service, config) {
    // ...
  }
}
